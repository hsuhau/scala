package chapter04

/**
 * while循环
 *
 * 1) 循环条件是返回一个布尔值的表达式
 * 2）while循环是先判断再执行语句
 * 3）与if语句不同，while语句本身没有值，即整个hile语句的结果是Unit类型的()
 * 4) 因为while中没有返回值，所以当要用该语句来计算并返回结果时，
 *    就不可避免地使用变量，而变量需要声明再while循环的外部，
 *    那么就等同于循环的内部对外部的变量造成了影响，
 *    所以不推荐使用，而是推荐使用for循环
 *
 *
 */
object Scala04_While {

}
